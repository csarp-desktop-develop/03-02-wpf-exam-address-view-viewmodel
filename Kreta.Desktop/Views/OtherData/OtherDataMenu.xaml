<UserControl x:Class="Kreta.Desktop.Views.OtherData.OtherDataMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:Kreta.Desktop.ViewModels.OtherData"
             d:DataContext="{d:DesignInstance Type=viewModels:OtherDataViewModel, IsDesignTimeCreatable=True}"
             xmlns:local="clr-namespace:Kreta.Desktop.Views.OtherData"
             mc:Ignorable="d" 
             d:DesignHeight="35" d:DesignWidth="800">
    <Border Background="{StaticResource backgroundColorThird}">
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button 
            Style="{StaticResource subMenuButton}"
            HorizontalAlignment="Left"
            Width="150"
            Margin="32 0 0 0"
            Content="Címek"
            Command="{Binding ShowAddressViewCommand}"
            />
        </StackPanel>
    </Border>
</UserControl>
